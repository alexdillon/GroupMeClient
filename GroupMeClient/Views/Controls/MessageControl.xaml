<UserControl x:Class="GroupMeClient.Views.Controls.MessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Ellipse Grid.Column="0" Grid.Row="0"  Grid.RowSpan="3" Width="40" Height="40" VerticalAlignment="Top">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding AvatarRound}" />
            </Ellipse.Fill>
        </Ellipse>

        <Label Content="{Binding Sender}" Grid.Column="1" Grid.Row="0" FontSize="12" FontFamily="Segoe UI" Foreground="#637182" Padding="0, 0, 0, 0" />

        <StackPanel Grid.Column="1" Grid.Row="1">
            <Image Source="{Binding ImageAttachment}" MaxWidth="500" HorizontalAlignment="Left"/>

            <Label  FontSize="15"  FontFamily="Segoe UI" Padding="0, 0, 0, 0">
                <TextBlock FontSize="15" TextWrapping="Wrap" Text="{Binding Text}" />
            </Label>
        </StackPanel>

        <Image Grid.Column="2" Grid.Row="1" Width="20" Height="20"/>
    </Grid>
</UserControl>
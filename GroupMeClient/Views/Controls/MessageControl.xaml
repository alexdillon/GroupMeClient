<UserControl x:Class="GroupMeClient.Views.Controls.MessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="100" d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Width="60" Height="60"/>

        
        <Label Content="{Binding Sender}" Grid.Column="1" Grid.Row="0" FontSize="12" FontFamily="Segoe UI" Foreground="#637182"/>

        <Label Grid.Column="1" Grid.Row="1" FontSize="15"  FontFamily="Segoe UI">
            <TextBlock TextWrapping="Wrap" Text="{Binding Text}" />
        </Label>
        
        <Image Grid.Column="2" Grid.Row="1" Width="20" Height="20"/>
    </Grid>
</UserControl>
<UserControl x:Class="GroupMeClient.Views.Controls.InlineReadSentMarkerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="400">

    <Grid Background="{Binding MessageColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="{Binding LabelString, Mode=OneTime}"
               HorizontalAlignment="Left"
               FontWeight="Regular"
               FontSize="11"
               Foreground="{DynamicResource MessageFontBrush}"
               Padding="0,0,0,0"
               Margin="55,-15,0,0"/>
    </Grid>
</UserControl>